---
title: "Next Solar Eclipse"
format: html

execute:
  echo: false
  warning: false
  freeze: auto
---

```{r}
library(dplyr)
library(readr)

source("format_data/Format_wikipedia_table.R") # bring the table here
```

```{r, import_data}
eclipse_data <- read_csv("data/formatted_eclipses.csv")
```
The next solar eclipse will be in:

```{r}
today <- Sys.Date()

next_eclipse_date <- mod_table |>
  mutate(Date = as.Date(Date)) |>
  filter(Date >= today) |>
  slice_min(Date, n = 1) |>
  pull(Date)

next_eclipse_date
```